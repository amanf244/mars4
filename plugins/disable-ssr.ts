// plugins/disable-ssr.ts
/**
 * Global plugin untuk disable SSR pada components tertentu
 * Ini membantu avoid hydration mismatch
 */

export default defineNuxtPlugin((nuxtApp) => {
  // Tidak ada yang perlu dilakukan di sini
  // Hanya untuk tracking
})
